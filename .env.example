# Optional: Add your GitHub Personal Access Token for higher rate limits
# Without it: 60 requests/hour (unauthenticated)
# With it:    5000 requests/hour
# Get one at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=

PORT=5000
